import { Meta } from '@storybook/react'
import { BadgeContainer } from '.'

export default {
  title: 'organisms/BadgeContainer',
  component: BadgeContainer,
} as Meta<typeof BadgeContainer>

export const Default = () => <BadgeContainer />
